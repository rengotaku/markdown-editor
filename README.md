# markdown-editor

Scaffolded from [my-boilerplate/react-spa-cloudflare](https://github.com/rengotaku/my-boilerplate/tree/main/react-spa-cloudflare).

## Getting Started

See the Makefile for available commands:

```bash
make help
```

## CI

CI workflow is at `.github/workflows/ci.yml`.

```bash
make ci
```
